name: Load Tests

on:
  workflow_dispatch:
  schedule:
    - cron: "0 3 * * 0"

jobs:
  k6:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: echo "Run k6 scripts from tests/load (placeholder)"
