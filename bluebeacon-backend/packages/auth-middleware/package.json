{
  "name": "@packages/auth-middleware",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "lint": "eslint src --ext .ts",
    "test": "node --test dist/**/*.test.js",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@packages/types": "file:../types",
    "express": "^5.1.0",
    "jsonwebtoken": "^9.0.2",
    "zod": "^4.3.6"
  }
}
